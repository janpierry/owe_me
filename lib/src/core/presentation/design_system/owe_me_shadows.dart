import 'package:flutter/material.dart';

class OweMeShadows {
  const OweMeShadows._();

  static const defaultShadow = [
    BoxShadow(
      color: Color.fromRGBO(0, 0, 0, 0.1),
      blurRadius: 16,
      offset: Offset(0, 4),
    ),
  ];
}
